explainer_selector
====================
This package contains a template implementation of the explainer selector node, which selects the most appropriate component explainer based on the context and the question posed.